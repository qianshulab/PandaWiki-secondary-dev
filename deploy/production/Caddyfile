{
    admin unix//app/run/caddy-admin.sock
    log {
        level INFO
        output stdout
    }
}

# Bootstrap route: will be replaced by backend sync config via Caddy admin API when KBs exist.
:80 {
    respond "PandaWiki Caddy is running, waiting for KB route synchronization."
}
